INCLUDE_DIRECTORIES (${POSHUKU_INCLUDE_DIR}
	${CMAKE_CURRENT_BINARY_DIR})
SET (PINTAB_SRCS
	pintab.cpp
	)
SET (PINTAB_HEADERS
	pintab.h
	)

IF (NOT LC_NO_MOC)
	QT4_WRAP_CPP (PINTAB_MOC_SRCS ${PINTAB_HEADERS})
ENDIF (NOT LC_NO_MOC)
ADD_LIBRARY (leechcraft_poshuku_pintab SHARED
	${PINTAB_SRCS}
	${PINTAB_MOC_SRCS}
	)
TARGET_LINK_LIBRARIES (leechcraft_poshuku_pintab
	${QT_LIBRARIES}
	${LEECHCRAFT_LIBRARIES}
	)
INSTALL (TARGETS leechcraft_poshuku_pintab DESTINATION ${LC_PLUGINS_DEST})
